@model IEnumerable<DDD.MVC.ViewModels.Client_ProductViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClientId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClientId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Client_ProductId }) |
            @Html.ActionLink("Details", "Details", new { id=item.Client_ProductId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Client_ProductId })
        </td>
    </tr>
}

</table>
